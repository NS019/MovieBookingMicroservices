import { CurrencyFormatPipeTsPipe } from './currency-format.pipe.ts-pipe';

describe('CurrencyFormatPipeTsPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyFormatPipeTsPipe();
    expect(pipe).toBeTruthy();
  });
});
